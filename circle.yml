machine:
  environment:
    GODIST: "go1.7.1.linux-amd64.tar.gz"
    IMPORT_PATH: "/home/ubuntu/.go_workspace/src/github.com/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME"
  post:
    - mkdir -p download
    - test -e download/$GODIST || curl -o download/$GODIST https://storage.googleapis.com/golang/$GODIST
    - sudo rm -rf /usr/local/go
    - sudo tar -C /usr/local -xzf download/$GODIST

dependencies:
  pre:
    # Install requirements
    - rake restore

test:
  override:
    - rake ci

  post:
    # add the coverage HTML report as CircleCI artifact
    - go tool cover -html=profile.cov -o $CIRCLE_ARTIFACTS/coverage.html
